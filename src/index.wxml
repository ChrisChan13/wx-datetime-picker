<picker
  mode="multiSelector"
  range="{{range}}"
  disabled="{{disabled}}"
  value="{{pickerValue}}"
  bindcolumnchange="onColumnChange"
  bindchange="onChange"
  bindcancel="onCancel"
>
  <view class="box-class">
    <slot />
  </view>
</picker>